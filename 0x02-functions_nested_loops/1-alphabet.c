#include "main.h"
/**
 * main - Print the alphabet in lowercase
 *Description: 'A function which prints the alphabet in lowercase, followed by a new line'
 *
 * Return: 0 (success)
 */
int main(void)
{
    print_alphabet(alphabet = 'a-z'; alphabet < z; a++);

    return (0);
}
